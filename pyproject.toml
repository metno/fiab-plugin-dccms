[build-system]
build-backend = "setuptools.build_meta"

requires = [ "setuptools>=80", "setuptools-scm[toml]>=6.2" ]

[project]
name = "fiab-plugin-dccms"
description = "Weather Forecast in a Box, DCCMS plugin"
readme = "README.md"
license = "Apache-2.0"
license-files = [ "LICENSE" ]
authors = [
  { name = "Norwegian Meteorological Institute (MET Norway)", email = "fiab@met.no" },
]
requires-python = ">=3.11"

dynamic = [ "version" ]

dependencies = [
    "fiab-core>=5,<6",
    "fiab-plugin-ecmwf>=5,<6"
]

[dependency-groups]
dev = [
  "ty==0.0.2",
  "pytest_lazy_fixtures",
]

[tool.setuptools_scm]
version_file = "src/fiab_plugin_dccms/_version.py"
write_to_template = '''# Do not change! Do not track in version control!
__version__ = "{version}"
'''
local_scheme = "no-local-version"
root = "."
fallback_version = "0.1.0"
tag_regex = '^[vd](?P<version>\d+(?:\.\d+){0,2}[^\+]*)(?:\+.*)?$'

[tool.ruff]
line-length = 140

lint.ignore = [ "E731", "ANN401" ]
lint.select = ["I", "ANN"]

[tool.pytest.ini_options]
log_cli = true
